<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="us.cpluspl.yonixw.talkingalarm.MainActivity"    >


    <TimePicker
        android:id="@+id/pickTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></TimePicker>

    <Button
        android:layout_width="wrap_content"
        android:layout_below="@+id/pickTime"
        android:layout_height="wrap_content"
        android:text="Speak"
        android:id="@+id/btnSpeak"
        android:onClick="clickspeak"/>

    <Button
        android:id="@+id/btnPlayTime"
        android:layout_toRightOf="@id/btnSpeak"
        android:layout_below="@id/pickTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play"
        android:onClick="clickPlay"
        />

    <Button
        android:id="@+id/btnStopClose"
        android:layout_toRightOf="@id/btnPlayTime"
        android:layout_below="@id/pickTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stay open"
        android:onClick="clickStopClose"
        />

    <TextView
        android:id="@+id/txtLoadPath"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnSpeak"
        android:background="@color/yello"
        android:text="PATH"/>
</RelativeLayout>
